```markdown
# 📸 Bookmarks - Платформа для обмена изображениями

<div align="center">

![Django](https://img.shields.io/badge/Django-4.2+-green.svg)
![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-13+-blue.svg)
![Redis](https://img.shields.io/badge/Redis-7.2+-red.svg)

**Социальная платформа для обмена изображениями с системой лайков, подписок и активностей**

[Установка](#установка) • [Функционал](#функционал) • [Архитектура](#архитектура) • [Документация](#документация)

</div>

## 🚀 О проекте

Bookmarks - это полнофункциональная социальная платформа, позволяющая пользователям делиться изображениями, находить интересный контент и взаимодействовать с другими пользователями. Система включает в себя продвинутую аутентификацию, ленту активностей и систему рекомендаций.

## ✨ Функционал

### 👤 Управление пользователями
- **Регистрация и аутентификация** - кастомная система с верификацией email
- **Расширенные профили** - фото, дата рождения, персональная информация
- **Автоматическое создание профилей** - через Django signals
- **Социальная аутентификация** - вход через Google и другие провайдеры

### 🖼️ Работа с изображениями
- **Загрузка и публикация** - поддержка форматов JPG, PNG, WebP
- **Система лайков** - интерактивная оценка контента
- **Рейтинг популярности** - автоматический расчет на основе активности
- **Букмарклет** - удобное расширение для браузера
- **Адаптивные превью** - автоматическое создание thumbnail'ов

### 🔗 Социальное взаимодействие
- **Система подписок** - следите за интересными авторами
- **Лента активностей** - персональные уведомления о действиях
- **Взаимные действия** - лайки, комментарии, шеринг
- **Рекомендации** - умная система подбора контента

### 📊 Аналитика и мониторинг
- **Тренды и статистика** - отслеживание популярного контента
- **Redis-кэширование** - высокая производительность
- **Панель отладки** - инструменты для разработчика

## 🏗️ Архитектура

### Структура каталогов
```
bookmarks/
├── 📁 account/                 # Аутентификация и профили
│   ├── models.py              # Модель Profile (расширение User)
│   ├── signals.py             # Сигналы для создания профилей
│   ├── authentication.py      # Кастомная аутентификация
│   └── templates/             # Шаблоны регистрации/входа
│
├── 📁 images/                 # Управление изображениями
│   ├── models.py              # Модель Image с системой лайков
│   ├── signals.py             # Сигналы для подсчета лайков
│   ├── utils.py               # Утилиты для обработки изображений
│   └── templates/             # Галереи и детальные просмотры
│
├── 📁 actions/                # Система активностей
│   ├── models.py              # Модель Action для ленты событий
│   ├── utils.py               # Утилиты для создания действий
│   └── templates/             # Отображение активности
│
├── 📁 bookmarks/              # Конфигурация проекта
│   ├── settings.py            # Настройки Django
│   ├── urls.py                # Корневые URL-маршруты
│   └── wsgi.py                # Конфигурация WSGI
│
├── 📁 media/                  # Пользовательские файлы
│   ├── images/                # Загруженные изображения
│   └── users/                 # Аватары пользователей
│
└── 📁 static/                 # Статические файлы
    ├── css/                   # Стили проекта
    └── js/                    # JavaScript компоненты
```

### Модели данных

#### 👤 Профиль пользователя (Profile)
```python
class Profile(models.Model):
    user = OneToOneField(User, on_delete=CASCADE)  # Связь 1-к-1
    date_of_birth = DateField(null=True, blank=True)
    photo = ImageField(upload_to='users/%Y/%m/%d/')
    created = DateTimeField(auto_now_add=True)
```

#### 🖼️ Изображение (Image)
```python
class Image(models.Model):
    user = ForeignKey(User, on_delete=CASCADE)     # Владелец
    title = CharField(max_length=200)              # Заголовок
    image = ImageField(upload_to='images/%Y/%m/%d/')
    description = TextField(blank=True)            # Описание
    created = DateTimeField(auto_now_add=True)     # Дата создания
    total_likes = PositiveIntegerField(default=0)  # Счетчик лайков
    users_like = ManyToManyField(User, related_name='images_liked', blank=True)
```

#### 📈 Действие (Action)
```python
class Action(models.Model):
    user = ForeignKey(User, on_delete=CASCADE)     # Инициатор
    verb = CharField(max_length=255)               # Действие
    target_ct = ForeignKey(ContentType, blank=True, null=True, on_delete=CASCADE)
    target_id = PositiveIntegerField(blank=True, null=True)
    target = GenericForeignKey('target_ct', 'target_id')  # Объект действия
    created = DateTimeField(auto_now_add=True)     # Время действия
```

### Технологический стек

| Компонент | Технология | Назначение |
|-----------|------------|------------|
| **Backend** | Django 4.2+ | Основной фреймворк |
| **База данных** | PostgreSQL | Продакшен база |
| **Кэширование** | Redis 7.2+ | Сессии, кэш, рейтинги |
| **Очереди** | Celery | Фоновые задачи |
| **Изображения** | Pillow | Обработка медиа |
| **Фронтенд** | HTML5/CSS3/JS | Пользовательский интерфейс |
| **Аутентификация** | Django-allauth | Социальный вход |

## 🛠️ Установка

### Предварительные требования
- Python 3.8+
- PostgreSQL 13+
- Redis 7.2+

### 1. Клонирование репозитория
```bash
git clone https://github.com/yourusername/bookmarks.git
cd bookmarks
```

### 2. Создание виртуального окружения
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate    # Windows
```

### 3. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 4. Настройка окружения
```bash
cp .env.example .env
```
Отредактируйте `.env` файл:
```env
DEBUG=True
SECRET_KEY=your-secret-key-here
DATABASE_URL=postgresql://user:password@localhost/bookmarks
REDIS_URL=redis://localhost:6379/0
ALLOWED_HOSTS=localhost,127.0.0.1
```

### 5. Настройка базы данных
```bash
# Применение миграций
python manage.py migrate

# Создание суперпользователя
python manage.py createsuperuser

# Загрузка тестовых данных (опционально)
python manage.py loaddata initial_data.json
```

### 6. Запуск сервисов
```bash
# Запуск Redis
docker run -d -p 6379:6379 redis:7.2

# Запуск разработческого сервера
python manage.py runserver
```

### 7. Доступ к приложению
Откройте в браузере: http://localhost:8000

## 📚 Документация

### Основные API endpoints

| Endpoint | Метод | Назначение |
|----------|--------|------------|
| `/account/register/` | POST | Регистрация пользователя |
| `/account/login/` | POST | Аутентификация |
| `/images/create/` | POST | Загрузка изображения |
| `/images/ranking/` | GET | Рейтинг изображений |
| `/actions/` | GET | Лента активностей |
| `/account/user/follow/` | POST | Подписка на пользователя |

### Сигналы и события

#### Автоматическое создание профиля
```python
@receiver(post_save, sender=User)
def create_user_profile(sender, instance, created, **kwargs):
    if created:
        Profile.objects.create(user=instance)
```

#### Обновление счетчика лайков
```python
@receiver(m2m_changed, sender=Image.users_like.through)
def users_like_changed(sender, instance, **kwargs):
    instance.total_likes = instance.users_like.count()
    instance.save()
```

## 📊 Производительность

### Оптимизация запросов
- **`select_related()`** - для ForeignKey связей
- **`prefetch_related()`** - для ManyToMany связей
- **Кэширование** - Redis для часто запрашиваемых данных
- **Пагинация** - бесконечный скролл для больших списков

### Мониторинг
- **Django Debug Toolbar** - отладка запросов
- **Redis monitoring** - отслеживание кэша
- **Query optimization** - анализ производительности БД

## 🤝 Разработка

### Установка для разработки
```bash
# Установка dev-зависимостей
pip install -r requirements/dev.txt

# Запуск тестов
python manage.py test

# Проверка кода
flake8 .
black .
```

### Коммиты и ветки
- `main` - стабильная версия
- `develop` - разработка
- `feature/` - новые функции
- `hotfix/` - срочные исправления

## 🐳 Docker развертывание

```bash
# Сборка и запуск
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

## 📈 Дорожная карта

- [ ] Мобильное приложение
- [ ] Система комментариев
- [ ] AI-рекомендации
- [ ] Monetization features
- [ ] Advanced analytics

## 📄 Лицензия

Этот проект лицензирован под MIT License - смотрите файл [LICENSE](LICENSE) для деталей.

---

<div align="center">

**⭐ Если проект был полезен, поставьте звезду на GitHub!**

*Разработано с ❤️ на Django*

</div>
```
